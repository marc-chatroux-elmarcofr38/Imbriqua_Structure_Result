//! Bounds
#![allow(unused_imports)]

/// Conversion of Bounds (DataType : Bounds)
#[derive(Debug, Clone)]
pub struct Bounds {
    pub x: Real,
    pub y: Real,
    pub width: Real,
    pub height: Real,
}

